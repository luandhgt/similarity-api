# Multi-LLM Provider Architecture

Visual guide về kiến trúc hệ thống Multi-LLM Provider.

---

## 🏗️ System Architecture

### High-Level Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     FastAPI Application                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │           ServiceContainer (DI Container)          │     │
│  ├────────────────────────────────────────────────────┤     │
│  │  Services:                                         │     │
│  │  - CLAUDE: LLMProvider (auto-selected)            │     │
│  │  - VOYAGE_CLIENT: VoyageClient                    │     │
│  │  - DATABASE: DatabaseService                      │     │
│  │  - EVENT_SIMILARITY: EventSimilarityService       │     │
│  │  - ABOUT_EXTRACTION: AboutExtractionService       │     │
│  └────────────────────────────────────────────────────┘     │
│                            │                                  │
│                            ▼                                  │
│  ┌────────────────────────────────────────────────────┐     │
│  │         ServiceFactory (Initialization)            │     │
│  ├────────────────────────────────────────────────────┤     │
│  │  _init_claude():                                   │     │
│  │    1. Read Config.LLM_PROVIDER                     │     │
│  │    2. Get provider config                          │     │
│  │    3. Call LLMProviderFactory                      │     │
│  │    4. Register in container                        │     │
│  └────────────────────────────────────────────────────┘     │
│                            │                                  │
└────────────────────────────┼──────────────────────────────────┘
                             │
                             ▼
         ┌──────────────────────────────────────────┐
         │      LLMProviderFactory (Factory)        │
         ├──────────────────────────────────────────┤
         │  create_provider(provider_type, ...)     │
         │  create_provider_from_config(config)     │
         │  get_cached_provider(...)                │
         └──────────────────┬───────────────────────┘
                            │
              ┌─────────────┴────────────┐
              │                          │
              ▼                          ▼
    ┌──────────────────┐      ┌──────────────────┐
    │  ClaudeProvider  │      │ ChatGPTProvider  │
    └────────┬─────────┘      └────────┬─────────┘
             │                         │
             └────────┬────────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │   BaseLLMProvider (ABC)    │
         ├────────────────────────────┤
         │  + generate_text()         │
         │  + analyze_image()         │
         │  + synthesize_content()    │
         │  + get_provider_info()     │
         └────────────────────────────┘
```

---

## 🔄 Provider Selection Flow

### Configuration Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Environment Variables (.env file)                            │
├─────────────────────────────────────────────────────────────────┤
│   LLM_PROVIDER=chatgpt                                          │
│   OPENAI_API_KEY=sk-proj-xxxxx                                  │
│   OPENAI_MODEL=gpt-4o                                           │
│   OPENAI_MAX_TOKENS=16384                                        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Config Class (config.py)                                     │
├─────────────────────────────────────────────────────────────────┤
│   Config.LLM_PROVIDER = "chatgpt"                               │
│   Config.OPENAI_API_KEY = "sk-proj-xxxxx"                       │
│   Config.OPENAI_MODEL = "gpt-4o"                                │
│   ...                                                            │
│                                                                  │
│   get_llm_provider_config() → returns:                          │
│   {                                                              │
│     "provider_type": "chatgpt",                                 │
│     "api_key": "sk-proj-xxxxx",                                 │
│     "model": "gpt-4o",                                          │
│     "max_tokens": 4096,                                         │
│     ...                                                          │
│   }                                                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. LLMProviderFactory                                            │
├─────────────────────────────────────────────────────────────────┤
│   create_provider_from_config(config):                          │
│     1. Extract provider_type = "chatgpt"                        │
│     2. Lookup in _PROVIDER_CLASSES registry                     │
│     3. Get ChatGPTProvider class                                │
│     4. Instantiate: ChatGPTProvider(api_key, model, ...)        │
│     5. Return instance                                          │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Provider Instance Created                                     │
├─────────────────────────────────────────────────────────────────┤
│   ChatGPTProvider(                                              │
│     api_key="sk-proj-xxxxx",                                    │
│     model="gpt-4o",                                             │
│     max_tokens=4096,                                            │
│     ...                                                          │
│   )                                                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Registered in ServiceContainer                                │
├─────────────────────────────────────────────────────────────────┤
│   container.register(ServiceNames.CLAUDE, chatgpt_instance)     │
│                                                                  │
│   Note: Named "CLAUDE" for backward compatibility               │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. Available in Application                                      │
├─────────────────────────────────────────────────────────────────┤
│   llm = container.get(ServiceNames.CLAUDE)                      │
│   # Returns ChatGPTProvider instance                            │
│                                                                  │
│   response = await llm.generate_text("Hello!")                  │
│   # Calls ChatGPTProvider.generate_text()                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔌 Request Flow

### API Request → LLM Response

```
┌──────────────────────────────────────────────────────────────┐
│  HTTP Request                                                 │
│  POST /api/about-extraction/extract                          │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  FastAPI Router (routers/about_extraction.py)                │
│  - Validate request                                          │
│  - Get container from app.state                              │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Get Service from Container                                  │
│  about_service = container.get(ServiceNames.ABOUT_EXTRACTION)│
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  AboutExtractionService                                       │
│  - Get LLM provider: container.get(ServiceNames.CLAUDE)      │
│  - Call llm.analyze_image()                                  │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  LLM Provider (ChatGPT/Claude)                                │
│  - Encode image to base64                                    │
│  - Build API request                                         │
│  - Call external API                                         │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  External API (OpenAI/Anthropic)                             │
│  - Process request                                           │
│  - Generate response                                         │
│  - Return JSON                                               │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  LLM Provider                                                 │
│  - Parse response                                            │
│  - Extract text                                              │
│  - Return to service                                         │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  AboutExtractionService                                       │
│  - Process LLM response                                      │
│  - Format as DTO                                             │
│  - Return to router                                          │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  FastAPI Router                                              │
│  - Wrap in APIResponse                                       │
│  - Return HTTP 200                                           │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  HTTP Response                                               │
│  { "success": true, "data": {...} }                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 🧩 Class Relationships

### Provider Hierarchy

```
┌─────────────────────────────────────────────────┐
│           BaseLLMProvider (ABC)                 │
│                                                 │
│  Abstract Interface                             │
├─────────────────────────────────────────────────┤
│  Properties:                                    │
│  + provider_type: LLMProviderType               │
│  + supports_vision: bool                        │
│                                                 │
│  Methods:                                       │
│  + generate_text(prompt, ...)                  │
│  + analyze_image(path, prompt, ...)            │
│  + analyze_multiple_images(paths, ...)         │
│  + synthesize_content(texts, ...)              │
│  + synthesize_with_images_and_texts(...)       │
│  + get_provider_info()                         │
└──────────────────┬──────────────────────────────┘
                   │
         ┌─────────┴────────┬──────────────┐
         │                  │              │
         ▼                  ▼              ▼
┌─────────────────┐ ┌──────────────┐ ┌──────────┐
│ ClaudeProvider  │ │ChatGPTProvider│ │ Gemini   │
├─────────────────┤ ├──────────────┤ ├──────────┤
│ Anthropic API   │ │  OpenAI API  │ │Google API│
├─────────────────┤ ├──────────────┤ ├──────────┤
│ Properties:     │ │Properties:   │ │ (Future) │
│ - api_key       │ │- api_key     │ │          │
│ - model         │ │- model       │ │          │
│ - base_url      │ │- base_url    │ │          │
│ - max_tokens    │ │- max_tokens  │ │          │
│                 │ │              │ │          │
│ Vision:         │ │Vision:       │ │          │
│ ✅ All models   │ │✅ GPT-4o     │ │          │
│                 │ │✅ GPT-4 Vision│ │          │
└─────────────────┘ └──────────────┘ └──────────┘
```

---

## 🏭 Factory Pattern

### Provider Registry & Creation

```
┌──────────────────────────────────────────────────┐
│         LLMProviderFactory                       │
├──────────────────────────────────────────────────┤
│                                                  │
│  _PROVIDER_CLASSES = {                           │
│    LLMProviderType.CLAUDE: ClaudeProvider,       │
│    LLMProviderType.CHATGPT: ChatGPTProvider,     │
│    LLMProviderType.GEMINI: GeminiProvider,       │
│  }                                               │
│                                                  │
│  _DEFAULT_MODELS = {                             │
│    CLAUDE: "claude-sonnet-4-5-20250929",         │
│    CHATGPT: "gpt-4o",                            │
│    GEMINI: "gemini-pro",                         │
│  }                                               │
│                                                  │
└──────────────────────────────────────────────────┘
         │
         │  create_provider(provider_type, ...)
         │
         ▼
┌──────────────────────────────────────────────────┐
│  Creation Flow:                                  │
│                                                  │
│  1. Parse provider_type string → Enum           │
│     "chatgpt" → LLMProviderType.CHATGPT         │
│                                                  │
│  2. Lookup in registry                          │
│     _PROVIDER_CLASSES[CHATGPT]                  │
│     → ChatGPTProvider class                     │
│                                                  │
│  3. Get default model if not provided           │
│     _DEFAULT_MODELS[CHATGPT]                    │
│     → "gpt-4o"                                  │
│                                                  │
│  4. Instantiate provider                        │
│     ChatGPTProvider(                            │
│       api_key=api_key,                          │
│       model=model,                              │
│       **kwargs                                  │
│     )                                           │
│                                                  │
│  5. Return provider instance                    │
│     → BaseLLMProvider                           │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 💾 Caching Mechanism

### Singleton Provider Cache

```
┌────────────────────────────────────────────────┐
│  Global Cache Dictionary                       │
│                                                │
│  _provider_cache = {                           │
│    "claude:claude-3-5-sonnet": <instance>,     │
│    "chatgpt:gpt-4o": <instance>,               │
│    "chatgpt:gpt-3.5-turbo": <instance>,        │
│  }                                             │
└────────────────────┬───────────────────────────┘
                     │
                     │  get_cached_provider()
                     │
                     ▼
┌────────────────────────────────────────────────┐
│  Cache Lookup Flow:                            │
│                                                │
│  1. Generate cache key                         │
│     f"{provider_type}:{model}"                 │
│     → "chatgpt:gpt-4o"                         │
│                                                │
│  2. Check if exists in cache                   │
│     if key in _provider_cache:                 │
│       return cached instance  ← Fast path      │
│                                                │
│  3. Not in cache → Create new                  │
│     instance = Factory.create_provider(...)    │
│                                                │
│  4. Store in cache                             │
│     _provider_cache[key] = instance            │
│                                                │
│  5. Return instance                            │
│                                                │
└────────────────────────────────────────────────┘

Benefits:
✅ Avoid recreating providers
✅ Reuse HTTP sessions
✅ Consistent configuration
✅ Better performance
```

---

## 📦 Dependency Injection

### ServiceContainer Integration

```
┌────────────────────────────────────────────────────┐
│             ServiceContainer                        │
│             (Singleton Pattern)                     │
├────────────────────────────────────────────────────┤
│                                                     │
│  _services = {                                      │
│    "CLAUDE": <LLMProvider instance>,                │
│    "VOYAGE_CLIENT": <VoyageClient>,                 │
│    "DATABASE": <DatabaseService>,                   │
│    "EVENT_SIMILARITY": <EventSimilarityService>,    │
│    ...                                              │
│  }                                                  │
│                                                     │
│  Methods:                                           │
│  - register(name, service)                          │
│  - get(name) → service                              │
│  - has(name) → bool                                 │
│  - is_initialized(name) → bool                      │
│                                                     │
└────────────────────────┬───────────────────────────┘
                         │
                         │  Injection Flow
                         │
         ┌───────────────┴──────────────┐
         │                              │
         ▼                              ▼
┌─────────────────┐         ┌────────────────────┐
│ Services use    │         │  Routes access     │
│ injected deps   │         │  via container     │
├─────────────────┤         ├────────────────────┤
│ class Service:  │         │ @router.post()     │
│   def __init__( │         │ async def route(   │
│     claude,     │         │   request: Request │
│     voyage,     │         │ ):                 │
│     db          │         │   container =      │
│   )             │         │     request.app    │
│                 │         │       .state       │
│                 │         │       .container   │
│                 │         │                    │
│                 │         │   llm = container  │
│                 │         │     .get(CLAUDE)   │
└─────────────────┘         └────────────────────┘

Benefits:
✅ Loose coupling
✅ Easy testing (mock injection)
✅ Single source of truth
✅ Lifecycle management
```

---

## 🔀 Switch Provider Diagram

### What happens when you change LLM_PROVIDER

```
BEFORE (Claude):
┌─────────────┐
│   .env      │
│ PROVIDER=   │
│   claude    │
└──────┬──────┘
       │
       ▼
┌─────────────┐    ┌─────────────────┐
│   Config    │───→│ ClaudeProvider  │
└─────────────┘    └────────┬────────┘
                            │
                            ▼
                   ┌────────────────┐
                   │ Anthropic API  │
                   └────────────────┘

AFTER (ChatGPT):
┌─────────────┐
│   .env      │
│ PROVIDER=   │
│  chatgpt    │  ← Changed here
└──────┬──────┘
       │
       ▼
┌─────────────┐    ┌──────────────────┐
│   Config    │───→│ ChatGPTProvider  │
└─────────────┘    └────────┬─────────┘
                            │
                            ▼
                   ┌────────────────┐
                   │  OpenAI API    │
                   └────────────────┘

Application Code: NO CHANGES NEEDED!
└──────────────────────────────────┘
```

---

## 🧪 Testing Architecture

### Test Isolation with Mocks

```
Production:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Service    │───→│ LLMProvider  │───→│External API  │
└──────────────┘    └──────────────┘    └──────────────┘

Testing:
┌──────────────┐    ┌──────────────┐
│   Service    │───→│ MockProvider │    (No external calls)
└──────────────┘    └──────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Predefined   │
                    │ Responses    │
                    └──────────────┘

Mock Example:
from unittest.mock import Mock

mock_provider = Mock(spec=BaseLLMProvider)
mock_provider.generate_text.return_value = "Mocked response"

container.register(ServiceNames.CLAUDE, mock_provider)
```

---

## 📊 Performance Considerations

### Request Lifecycle Performance

```
┌────────────────────────────────────────────────┐
│  Performance Hotspots                          │
├────────────────────────────────────────────────┤
│                                                │
│  1. Provider Initialization (~0ms)             │
│     - Only happens once (singleton)            │
│     - Cached in ServiceContainer               │
│     ✅ No overhead per request                 │
│                                                │
│  2. Provider Selection (~0ms)                  │
│     - Dict lookup in container                 │
│     - O(1) complexity                          │
│     ✅ Negligible overhead                     │
│                                                │
│  3. Image Encoding (~10-50ms)                  │
│     - Base64 encoding                          │
│     - Depends on image size                    │
│     ⚠️  Can be optimized with caching          │
│                                                │
│  4. API Call (~500-3000ms)                     │
│     - Network latency                          │
│     - Model processing time                    │
│     ⚠️  Main bottleneck                        │
│                                                │
│  5. Response Parsing (~1-5ms)                  │
│     - JSON parsing                             │
│     - String extraction                        │
│     ✅ Fast                                    │
│                                                │
└────────────────────────────────────────────────┘

Total per request: ~500-3100ms
Provider abstraction overhead: <1ms (0.03%)
```

---

## 🔐 Security Architecture

### API Key Management

```
┌────────────────────────────────────────────────┐
│  Security Layers                               │
├────────────────────────────────────────────────┤
│                                                │
│  1. Environment Variables                      │
│     .env (gitignored)                          │
│     ├─ CLAUDE_API_KEY=sk-ant-xxx              │
│     ├─ OPENAI_API_KEY=sk-proj-xxx             │
│     └─ GEMINI_API_KEY=xxx                     │
│                                                │
│  2. Config Loading (startup)                   │
│     config.py                                  │
│     - Load from .env                           │
│     - Validate presence                        │
│     - Never log keys                           │
│                                                │
│  3. Provider Instantiation                     │
│     - Keys passed to constructor               │
│     - Stored in private attributes             │
│     - Never exposed in responses               │
│                                                │
│  4. API Requests                               │
│     - Keys in headers only                     │
│     - HTTPS required                           │
│     - No key in URL/logs                       │
│                                                │
└────────────────────────────────────────────────┘

Best Practices:
✅ .env in .gitignore
✅ Different keys dev/prod
✅ Rotate keys regularly
✅ Monitor usage dashboards
```

---

## 🎯 Summary

### Key Design Principles

1. **Abstraction:** Single interface for all providers
2. **Factory:** Centralized creation logic
3. **Singleton:** Cached instances
4. **Dependency Injection:** Loose coupling
5. **Configuration:** Environment-based selection
6. **Backward Compatibility:** No breaking changes

### Files Structure

```
services/
├── llm_provider_base.py      (Abstract interface)
├── claude_provider.py         (Claude implementation)
├── chatgpt_provider.py        (ChatGPT implementation)
└── llm_provider_factory.py   (Factory + caching)

core/
└── service_factory.py         (Integration point)

config.py                      (Multi-provider config)
.env                          (API keys + selection)
```

---

**Architecture Version:** 2.1.0
**Design Pattern:** Strategy + Factory + Singleton + DI
**Status:** ✅ Production Ready
